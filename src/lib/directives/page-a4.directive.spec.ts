import { PageA4Directive } from './page-a4.directive';

describe('PageA4Directive', () => {
  it('should create an instance', () => {
    const directive = new PageA4Directive();
    expect(directive).toBeTruthy();
  });
});
