import { MultirowDirective } from './multirow.directive';

describe('MultirowDirective', () => {
  it('should create an instance', () => {
    const directive = new MultirowDirective();
    expect(directive).toBeTruthy();
  });
});
